---
cairn: tasks
change: pimalaya-logo
---

# Tasks

- [x] Logo.tsx: inline the official paths + gradient, per-variant gradient id
- [x] Logo.css: drop the placeholder fill overrides (mark keeps its gradient)
- [x] public/favicon.svg: copy the official logo, add viewBox
- [x] Fold spec (`structure`), rebuild and verify
