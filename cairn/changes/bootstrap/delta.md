---
cairn: delta
change: bootstrap
---

# Delta

## ADDED Requirements

All capabilities are new: `structure` (JavaScript-free prerendered pages, page
set, shared letterpress chrome, links in config), `posts` (one markdown file
per post, frontmatter contract, build-time rendering, index order), `feed`
(RSS 2.0 at /feed.xml, discoverability, reproducible output), and `publishing`
(rebuild on push, newsletter mirrors new posts, deployment target).

## MODIFIED Requirements

None.

## REMOVED Requirements

None.
