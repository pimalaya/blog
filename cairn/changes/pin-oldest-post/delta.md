---
cairn: delta
change: pin-oldest-post
---

# Delta

## ADDED Requirements

None.

## MODIFIED Requirements

- `structure` / Pinned post hero → First post as hero: the index hero is the
  oldest post by date, derived implicitly; no frontmatter flag.
- `posts` / Frontmatter contract: `pinned` removed; only `draft: true` remains
  optional.

## REMOVED Requirements

None.
